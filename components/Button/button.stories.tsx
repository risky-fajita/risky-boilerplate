import { Button } from '.';

export default {
  title: 'Buttons/Primary Button',
  component: Button,
};

export const Primary = {
  args: {
    variant: 'primary',
    text: 'Primary Button',
  },
};
